# Docker Containerization - Part 3 (CLI Operations)
Today I mastered the core infrastructure command workflows.
1. Formulated runtime metadata schemas for docker build, docker run, and process pruning operations.
2. Isolated the architectural boundaries between static Docker Images and active runtime Containers.

